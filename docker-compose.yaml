version: "3.6"
services:
  react:
    image: node:latest
    volumes:
      - ./react:/usr/app
    working_dir: /usr/app
    ports:
      - 8080:8080
    environment:
      - APP_PORT
    entrypoint: "yarn start"